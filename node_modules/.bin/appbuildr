#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../appbuildr/bin/appbuildr.js" "$@"
  ret=$?
else 
  node  "$basedir/../appbuildr/bin/appbuildr.js" "$@"
  ret=$?
fi
exit $ret
